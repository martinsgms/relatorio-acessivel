server:
  port: 8081

eureka:
  instance:
    preferIpAddress: true
    hostname: svc-app-usuario
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://svc-eureka-server/eureka
