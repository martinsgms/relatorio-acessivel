server:
  port: 8888

spring:
  application:    name: config-server
  cloud:    config:
      server:        git:
          uri: https://github.com/martinsgms/relatorio-inteligente-config.git
          search-paths: '{application}'
